const WinnerViewer = () => (
  <div className="popup-window">
    <div>Winner</div>
    <button type="button" className="change-btn">
      Change name
    </button>
  </div>
);

export default WinnerViewer;
